import styled from "styled-components"

export default styled.div`
  .bike {
    display: flex;
    align-items: center;
  }
  image {
    width: 300px;
    height: 300px;
    object-fit: cover;
  }
`
